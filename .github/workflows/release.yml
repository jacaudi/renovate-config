name: Release

on:
  push:
    branches: [main]

jobs:
  release:
    uses: jacaudi/github-actions/.github/workflows/component-semantic-release.yml@v0.20.3
    permissions:
      contents: write
      issues: write
      pull-requests: write
